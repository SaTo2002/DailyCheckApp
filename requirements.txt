# --- Core Flask & Security ---
Flask==3.0.3
python-dotenv==1.0.1

# --- Database (MySQL) ---
Flask-SQLAlchemy==3.1.1
PyMySQL==1.1.1

# --- Image Processing ---
Pillow

# --- Excel Generation ---
openpyxl==3.1.5

# --- Password Hashing ---
werkzeug

# --- Windows PDF Export (win32com) ---
# Only needed on Windows — install manually:
# pip install pywin32
# (Not needed on Linux — LibreOffice is used instead)
